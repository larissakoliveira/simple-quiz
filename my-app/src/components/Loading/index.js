import loading from "../../assets/loading.gif";
import { LoadingImage } from "./style";

const Loading = () => {
 
  return (
    <LoadingImage src={loading} alt='loadingImage'/>
  );
};

export default Loading;
